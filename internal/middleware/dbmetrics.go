package middleware

func DbMetrics() {
	//now := time.Now()

	// some repo logic

	//elapsedSeocnds := time.Since(now).Seconds()

	//metrics.DbQueryDuration.WithLabelValues(pattern, method, status).Inc()
	//metrics.DbQueryErrors.WithLabelValues(pattern, method).Observe(elapsedSeocnds)
	//metrics.DbQueriesTotal.WithLabelValues(pattern, method).Observe(elapsedSeocnds)
}
